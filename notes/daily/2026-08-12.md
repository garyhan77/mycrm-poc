---
tags: [daily, crm-poc]
---

# 2026-08-12

Kicked off the [[../00-index|CRM PoC]]. Confirmed stack roles (NestJS backend, Next.js frontend), scoped to a single `Customer` entity with no auth, local-first hosting with Azure deferred.

Scaffolded the workspace root, `.gitignore`, and this `notes/` tree. Removed a stray default Obsidian vault (`CRM Project/`) that had been created inside `CRM PoC/` with no real content.

Next: install MySQL via Homebrew and scaffold `apps/api` and `apps/web`.
